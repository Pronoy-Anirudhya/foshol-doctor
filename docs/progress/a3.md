# A3 · analysis

## Done
- ANALYSIS-FR-070 … `076` speech branch on diagnosis cases (existing)
- Voice KDB catalogue lookup command `LookupVoiceKbCommand` (candidates only, no `analysis_run`, no `AnalysisCompleted`)
- `POST /api/v1/faq/voice-search` (role FARMER) — OpenAPI + `FaqVoiceController`
- Farmer FAQ screen at `/` (confirm disease, then `GET /api/v1/diseases/{id}/remedies`)

## In progress

## Blocked
- Full Angular farmer/officer/admin app (`web/`, `70-frontend.ears.md`) · owner **A6** · Angular pin token `22.1.5` still in `00-common` (`COMMON-NFR-004`) · since 2026-09-10

Catalogue remedies remain farmer-readable on the existing GET. Voice FAQ does not publish a case advisory. Field diagnosis remains `POST /api/v1/cases` + officer HITL (ADR-0003).
