## Done
INTAKE-FR-001 WebIntakeAdapter is the sole CaseIntakeChannel
INTAKE-FR-003 content-type treated as hint; sniffer is authoritative
INTAKE-FR-004 audio/mp4 ftyp signature
INTAKE-FR-010..015 submission, farmer-from-JWT, image count, crop, NFC note, audio duration
INTAKE-FR-020..028 quality gate, SHA-256, object keys, derivatives, primary image
INTAKE-FR-030..037 idempotency replay/conflict/race, CaseSubmitted after commit
INTAKE-FR-038..046 status machine listeners (CaseSubmitted, AnalysisCompleted/Failed, AdvisoryApproved, CaseRejected)
INTAKE-FR-050..052 parentCaseId resubmission
INTAKE-FR-060..064 p_farmer_case_history upsert + advisory/rejection enrichment
INTAKE-FR-070..072 reads, 302 `/content`, JSON `/url`, no object keys in farmer JSON
INTAKE-SEC-001..008 sniffing, size, filenames, rate limit 20/PT1H, ownership 404 not 403
INTAKE-DATA-001..005,007..010 status writer, keys, sha256, raw UUIDs
INTAKE-API-001 CaseIntakeApi findById / isOwnedBy / recordTranscript
INTAKE-NFR-001..004 idempotent listeners, in-process quality, storage 503, optimistic lock retry
INTAKE-UX-001 quality problem document HTTP 422 `QualityGateProblem` with UNDEREXPOSED/OVEREXPOSED

## In progress
App-level photo → ReviewTask path is `PhotoToReviewTaskIT`. Module IT remains tagged `integration`.

## Blocked
INTAKE-FR-002 [DEFERRED] SmsIntakeAdapter — out of scope
INTAKE-DATA-006 [DEFERRED] case_image.rejected_reason unused (all-or-nothing gate)
QualityReason belongs in common per COMMON-ARCH-010 — declared in intake.domain until A1 ships it
INTAKE-NFR-006 plant-photograph fixtures / tools/build_fixtures.py — generated high-contrast JPEGs in tests instead
INTAKE-UX-002 web/src/assets/i18n quality strings — A6; intake MessageSource bundles supplied for API detail
`@ReadOnlyDataSource` not shipped (A1) — JDBC queries use the primary DataSource
Identity JWT filter not present in this module; caller is read from SecurityContext (`sub` = farmer id, `ROLE_FARMER`)
Gradle evaluation of this repo is blocked when `modules/analysis/build.gradle` is missing the `dependencies { }` closing brace (A3 ownership) — `./gradlew :modules:intake:test` cannot run until that file parses
