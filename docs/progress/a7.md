# A7 — inference sidecar and tools

## Done
SIDECAR-FR-001 replay `GET /health` reports `mode: "REPLAY"` and `status: "UP"`
SIDECAR-FR-005 replay loads no weights; Dockerfile has no torch/transformers and sets `HF_HUB_OFFLINE=1`
SIDECAR-FR-007 `X-Foshol-Mode` and body `mode` on responses
SIDECAR-FR-010 classify returns `{raw_label, confidence, rank}` top-k descending
SIDECAR-FR-011 crop_code routing via `FOSHOL_AI_VISION_CROP_ROUTES` (opaque keys rice/tomato/potato)
SIDECAR-FR-012 unknown crop_code → 400 `ERR_SIDECAR_UNKNOWN_CROP`
SIDECAR-FR-013 confidence in `[0,1]` to four decimal places
SIDECAR-FR-014 `model_id` / `model_version` from env of the model that routed
SIDECAR-FR-015 `image_sha256` is SHA-256 of posted bytes
SIDECAR-FR-016 crop_code treated as opaque; fixtures use `class_*` raw_label strings only (no project taxonomy)
SIDECAR-FR-022 `model_role=fallback` selects rice fallback identity
SIDECAR-FR-030 / FR-073 replay explain serves pre-baked PNG
SIDECAR-FR-036 overlay headers `X-Foshol-Model-Id|Version|Target-Label|Method|Mode`
SIDECAR-FR-045 silent ASR fixture → empty transcript, `confidence: 0.000`, `speech_detected: false`
SIDECAR-FR-050 / FR-052 embed returns 768-d L2-normalised vectors (hash unit vectors in replay when no fixture)
SIDECAR-FR-051 NFC, ZWNJ/ZWJ strip, Bengali digit fold
SIDECAR-FR-053 embed batch over max → 413
SIDECAR-FR-055 no `/v1/symptoms/extract` route
SIDECAR-FR-060 health fields `status, mode, warm, models_loaded, models_expected, degraded_reasons`
SIDECAR-FR-062 `GET /v1/models` lists five registry roles
SIDECAR-FR-070…074 SHA-256 fixture store under `sidecar/fixtures/` (classify/explain/asr); missing → 404 `ERR_SIDECAR_FIXTURE_MISSING`
SIDECAR-FR-081 `build_fixtures.py` refuses a REPLAY sidecar
SIDECAR-API-001 RFC 9457 `application/problem+json`
SIDECAR-API-002 `X-Correlation-Id` adopted or generated
SIDECAR-API-003 FastAPI `/openapi.json`
SIDECAR-NFR-004 inference semaphore / 429 `ERR_SIDECAR_BUSY`
SIDECAR-NFR-005 single uvicorn worker
SIDECAR-NFR-009 one structured INFO line per request; no image/audio/transcript bytes logged
SIDECAR-NFR-020 config env reads only in `sidecar/app/config.py` (also accepts previous compose aliases)
SIDECAR-NFR-021 error codes only in `sidecar/app/errors.py`
SIDECAR-SEC-001 no database driver
SIDECAR-SEC-004 magic-byte sniffing for image/audio
SIDECAR-SEC-006 non-root container user
SIDECAR-SEC-007 no request-time HF download (`HF_HUB_OFFLINE=1`); live without weights → 503 `ERR_SIDECAR_MODEL_UNAVAILABLE`
SIDECAR-SEC-008 committed fixtures are synthetic 1×1/1×2 PNG and silent WAV (no faces, voices, EXIF)
SIDECAR-DATA-001 fixture layout committed
SIDECAR-FR-080 helper `tools/build_fixtures.py --image` writes vision classify JSON + explain PNG
SIDECAR-FR-090…097 `tools/eval.py` writes `docs/eval-report.md`; 0 samples / deferred when no held-out set
SIDECAR-FR-100…107 `tools/seed_cases.py` stub: count 40–60, deterministic `--seed`, no agronomic content, no identity rows
COMMON-NFR-006 embed dimension 768
`docker-compose.yml` sidecar env names aligned to `FOSHOL_AI_VISION_*` / `FOSHOL_AI_ASR_*` / `FOSHOL_AI_EMBED_*`
pytest: 24 passed (classify replay, missing fixture, unknown crop, health, embed, explain, ASR, routing, tools)

## In progress
Live weight load remains Day-2+ when HF weights are present locally; replay path is the demo default.
SIDECAR-FR-075 / NFR-017 live suite in `verify-live.yml`

## Blocked
SIDECAR-DATA-009 label-map CSV `tools/eval/label-map.csv` · A1 / C13 · 2026-09-07
SIDECAR-FR-095 held-out `tools/eval/manifest.csv` labels · content owner C14 · 2026-09-07
SIDECAR-FR-107 V100 demo identities for a real seed run · A1 · 2026-09-07
SIDECAR-NFR-016 pytest not wired into `.github/workflows/verify.yml` · A1 (CI) · 2026-09-07
SIDECAR-FR-006 process-exit when weights fail to load · Day-1 choice is 503 without downloading HF weights · revisit with A1 if start-up failure is required in live

## Deferred
SIDECAR-FR-110 `POST /v1/vision/describe` VLM path
SIDECAR-NFR-015 GPU device selection
