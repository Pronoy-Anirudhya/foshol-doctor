spring.datasource.url=${DB_URL}
spring.datasource.username=${FOSHOL_DB_USER:foshol}
spring.datasource.password=${FOSHOL_DB_PASSWORD}
foshol.datasource.read-only.url=${DB_URL}
foshol.ai.mode=live
foshol.ai.base-url=${SIDECAR_BASE_URL}
foshol.storage.endpoint=${MINIO_ENDPOINT}
