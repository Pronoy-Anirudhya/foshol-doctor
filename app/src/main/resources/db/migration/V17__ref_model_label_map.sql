-- TODO(content-owner: C13) (model_id, model_version, raw_label) -> disease_id.
-- Mapping two label strings onto the same disease is an agronomic judgement (ADR-0009).
-- Unmapped raw labels route to UNDETERMINED (COMMON-DATA-012 / COMMON-DATA-013).
SELECT 1;
